const numero1 = 20;
const numero2 = "20";


console.log(parseInt(numero2)); //parseInt() es una funcion

console.log(numero1.toString()); //.toString es un metodo. 
//NOTA: los que tien un punto y luego la funcion se les llama METODOS.  
// y los que tiene el nombre de la funcion como tal, con los parentesis son llamados funciones.